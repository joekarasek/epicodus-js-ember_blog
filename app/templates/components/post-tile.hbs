<div>
  <h2>{{ post.title }}</h2>
  <h4>By: {{ post.author }}</h4>
  <img src={{post.image}} alt={{post.title}} >
  <p>{{ post.body }}</p>
  <button {{action 'delete' post}}>Delete Post</button>
</div>
